<div class="mt-2">
  <h1 align="center"><%= t('.header') %></h1>
</div>

<%= button_to t('.new'), new_admin_test_path, class: 'btn btn-dark ml-1', method: :get  %>

<div class="mt-2">
  <table class="table table-striped table-bordered">
    <thead class="thead-dark" align="center">
      <th><%= t('.title') %></th>
      <th><%= t('.level') %></th>
      <th><%= t('.questions') %></th>
      <th colspan="3"><%= t('.actions') %></th>
    </thead>

  <%= render @tests %>
  </table>
</div>

<nav class="navbar navbar-dark bg-dark navbar-expand-lg">

    <div class="collapse navbar-collapse">
      <%= link_to "Test-Guru", root_path, class: 'navbar-brand' %>
      <li class="navbar-brand">
        <a href="/?lang=ru"><span class="flag-icon flag-icon-ru flag-icon-squared"></span></a>
      </li>
      <li class="navbar-brand">
        <a href="/?lang=en"><span class="flag-icon flag-icon-us flag-icon-squared"></span></a>
      </li>
      <% if user_signed_in? && !current_user.is_admin? %>
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <%= link_to t('.feedback'), new_feedback_path, class: 'nav-link'%>
          </li>
        </ul>
      <% end %>
      <% if user_signed_in? && current_user.is_admin? %>
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <%= link_to t('.panel'), admin_tests_path, class: 'nav-link'%>
        </li>
        <li class="nav-item active">
          <span class="flag-icon flag-icon-en flag-icon-squared"></span>
        </li>
      </ul>
      <% end %>
    </div>

  <span class="navbar-text">
    <% if user_signed_in? %>
      <%= greeting %> |
      <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to t('.register'), new_user_registration_path %>  |
      <%= link_to t('.login'), new_user_session_path %>
    <% end %>
  </span>
</nav>
